apiVersion: v1
kind: Service
metadata:
  name: task-frontend
  namespace: task-app
spec:
  selector:
    app: task-app
    component: frontend
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30081
  type: NodePort
