apiVersion: v1
kind: Service
metadata:
  name: task-backend
  namespace: task-app
spec:
  selector:
    app: task-app
    component: backend
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
